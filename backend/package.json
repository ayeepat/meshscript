{
  "name": "smesh-licenses",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "License issuance + verification backend for the СМЭШ AI extension.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
